# Personnalisation de Nginx pour éviter les boucles de redirection
location / {
    try_files $uri $uri/ /index.php?$query_string;
}

# Supprimer ou modifier le comportement @rewrite qui cause l'erreur
location @rewrite {
    return 301 /index.php;
}
